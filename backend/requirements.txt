# Backend dependencies
# Core web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.5.0

# LangGraph for agent orchestration
langgraph>=0.2.0
langchain-core>=0.2.0

# OpenAI client (for GreenNode MAAS - OpenAI compatible)
openai>=1.12.0

# SQLite for local fallback
aiosqlite>=0.19.0

# LanceDB for local vector store (fallback)
lancedb>=0.8.0

# For SSE streaming
sse-starlette>=2.0.0

# Logging
python-json-logger>=2.0.0

# Utilities
httpx>=0.26.0
tenacity>=8.2.0

# Code formatting and linting
ruff>=0.3.0
black>=24.2.0
isort>=5.13.0

# Development
pytest>=8.0.0
pytest-asyncio>=0.23.0
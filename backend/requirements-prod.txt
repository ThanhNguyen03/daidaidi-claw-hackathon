# Backend dependencies — server deployment (zah-28.123c.vn)
# ==========================================================
# Trimmed from requirements.txt for a smaller image and faster builds.
# Removed: torch + sentence-transformers (only used by the LOCAL embedding
# fallback; production uses KB_EMBEDDING_PROVIDER=greennode), unused
# langgraph/langchain-core/sse-starlette/python-json-logger/aiosqlite, and
# dev-only tooling (ruff/black/isort/pytest).

# Core web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.5.0

# OpenAI client (GreenNode MAAS is OpenAI-compatible)
openai>=1.12.0

# Vector store for RAG
lancedb>=0.8.0

# HTTP + retries
httpx>=0.26.0
tenacity>=8.2.0

# NOTE: pyyaml is a HARD requirement, not optional. tools/ingest.py reads
# config/agents.yaml to decide which agents to index; if the import fails it
# silently returns [] and the whole knowledge base is never ingested.
pyyaml>=6.0

numpy>=1.24.0

# PPTX generation
python-pptx>=0.6.23

# Rate limiting
slowapi>=0.1.9

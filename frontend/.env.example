# Frontend environment
# Copy to `.env.production` for local production testing, or set the same
# values in Vercel project settings.

# Backend / AgentBase API base URL
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
